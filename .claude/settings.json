{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); if echo \"$INPUT\" | grep -q 'git commit'; then bash .claude/hooks/check-help-update.sh; fi"
          }
        ]
      }
    ]
  }
}
